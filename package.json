{
  "name": "gym-pwa",
  "version": "1.0.0",
  "description": "Personal gym tracking Progressive Web App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild node_modules/@insforge/sdk/dist/index.mjs --bundle --format=esm --platform=browser --outfile=public/js/vendor/insforge-sdk-bundle.mjs",
    "test": "echo 'No tests yet' && exit 0"
  },
  "dependencies": {
    "@insforge/sdk": "^1.2.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "puppeteer": "^24.40.0"
  }
}
